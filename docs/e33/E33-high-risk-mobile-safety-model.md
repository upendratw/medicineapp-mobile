# E33 High-risk Mobile Safety Model

Symptoms are input collection only: no local diagnosis, triage, or medication advice; validated backend output is required and currently pending. SOS is explicitly user initiated, opens only an OS dialer after confirmation, and never implies dispatch, auto-contact, SMS, or location sharing. Voice recognition is untrusted input: deterministic navigation is allowed, while emergency and medication actions require confirmation and clinical commands are blocked.
