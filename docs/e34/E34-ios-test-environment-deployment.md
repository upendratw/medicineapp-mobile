# iOS test environment deployment

Preview/test configuration is ready for an internal iOS build using the linked EAS project, test environment, diagnostics disabled, and `https://api-medicine.ankala.ai`. Actual signed build and device execution are PENDING Apple credentials/authorization. TestFlight upload was NOT PERFORMED. Android preview configuration remains intact.
